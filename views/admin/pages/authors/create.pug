extends ../../layouts/default
include ../../mixins/alert.pug

block main
  if(role.permissions.includes("authors_create"))
    +alert-success(5000)
    +alert-error(5000)

    div(class="card")
      div(class="card-header d-flex justify-content-between align-items-center")
        h5(class="mb-0") Thêm tác giả
        div(class="d-flex align-items-center gap-2")
          a(href=`${prefixAdmin}/authors` class="btn btn-outline-secondary") Quay lại danh sách
          button(type="submit" form="form-author" class="btn btn-primary")
            i(class="bi bi-plus-lg mr-1")
            | Thêm mới

      div(class="card-body")
        form(id="form-author" action=`${prefixAdmin}/authors/create` method="POST")
          div(class="card")
            div(class="card-header bg-light text-dark font-weight-semibold") Thông tin tác giả
            div(class="card-body")

              div(class="form-group row align-items-center mb-3")
                label(class="col-sm-3 col-form-label") Tên tác giả *
                div(class="col-sm-9")
                  input(type="text" name="fullName" class="form-control" placeholder="Nhập tên tác giả" required)

              div(class="form-group row align-items-center mb-3")
                label(class="col-sm-3 col-form-label") Mô tả
                div(class="col-sm-9")
                  textarea(name="description" rows="3" class="form-control" placeholder="Nhập mô tả (nếu có)")
